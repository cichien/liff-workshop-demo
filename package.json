{
  "name": "liff-demo",
  "version": "1.0.0",
  "description": "This is a small web application",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cross-env NODE_ENV=development REDIRECT_URI=https://localhost:8000 node index.js",
    "start": "node index.js"
  },
  "license": "MIT",
  "dependencies": {
    "cross-env": "^7.0.2",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "pg": "^8.4.1"
  },
  "engines": {
    "node": "12.x"
  }
}
